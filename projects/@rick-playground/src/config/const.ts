export const TabHeight = 32;

export const SideMinWidth = 0;
export const SideMaxWidth = window.innerWidth * 0.7;
export const SideMaxHeight = window.innerHeight * 0.8;
export const SideInitWidth = window.innerWidth * 0.5;
