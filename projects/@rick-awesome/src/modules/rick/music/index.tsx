import VueComponentWrapper from '@rickzhou/awesome/components/vue-component';
import Cpn from './index.v2.vue';

const Music = () => {
  return <VueComponentWrapper vue={Cpn} version={2} />;
};

export default Music;
